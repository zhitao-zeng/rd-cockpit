# 后端只读 API 地址。默认 http://127.0.0.1:8787
# 开发模式（npm run dev）下由 .env.development 置空、走 Vite 同源代理，避免 CORS。
# 生产部署时按实际 API 地址设置，例如：
# Leave empty in development to use the Vite /api proxy.
VITE_API_BASE_URL=
# Optional proxy target override.
VITE_API_PROXY_TARGET=http://127.0.0.1:8787
